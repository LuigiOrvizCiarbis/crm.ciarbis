// Shared importer entry point. Resource-specific screens configure this same
// dialog through `resource`, native targets and field definitions.
export { ImportProductsDialog as UniversalImportDialog } from "@/components/products/import-products-dialog"
